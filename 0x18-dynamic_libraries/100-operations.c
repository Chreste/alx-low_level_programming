#include <stdio.h>

int add(int i, int x)
{
	return i + x;
}

int sub(int i, int x)
{
	return i - x;
}

int mul(int i, int x)
{
	return i * x;
}

int div(int i, int x)
{
	return i / x;
}

int mod(int i, int x)
{
	return i % x;
}
